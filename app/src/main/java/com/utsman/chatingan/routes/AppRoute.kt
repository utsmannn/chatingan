package com.utsman.chatingan.routes

import com.utsman.chatingan.navigation.Route

object AppRoute {
    object Main : Route("main")
    object Login : Route("login")
    object Splash : Route("splash")
}